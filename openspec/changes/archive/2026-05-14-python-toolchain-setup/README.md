# python-toolchain-setup

Configure and integrate linting (ruff→flake8/pylint/pyright), security (bandit), and dependency auditing (pip-audit); fix all warnings and errors across prism-core and prism-cli
